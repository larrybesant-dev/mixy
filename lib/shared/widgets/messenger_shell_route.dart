import 'package:flutter/material.dart';

class MessengerShellRoute extends StatelessWidget {
  const MessengerShellRoute({super.key, required this.child});
  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
